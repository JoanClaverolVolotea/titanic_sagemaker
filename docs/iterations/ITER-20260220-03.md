# ITER-20260220-03

## Objetivo y contexto
Alinear todos los tutoriales `docs/tutorials/00-07` con documentacion oficial de AWS y referencia local `docs/aws/sagemaker-dg.pdf`, dejando trazabilidad de revisiones one-by-one.

## Decisiones tecnicas y alternativas descartadas
1. Se mantiene `docs/aws/sagemaker-dg.pdf` como referencia local transversal para todas las fases.
2. Se agregaron/normalizaron secciones de `Fuentes oficiales` en fases que no las tenian (`00`, `01`, `02`).
3. Se corrigieron hardcodes de bucket en `02-training-validation.md` para usar `DATA_BUCKET` y evitar drift por cuenta/entorno.
4. Se homogeneizo la referencia a `docs/aws/sagemaker-dg.pdf` en fases `03` a `07`.
5. Alternativa descartada: dejar alineacion solo en fases `03+` sin completar fuentes oficiales en `00-02`.

## IAM usado (roles/policies/permisos clave)
1. Identidad base de todas las verificaciones: `data-science-user`.
2. No se introducen nuevas policies ni cambios de infraestructura; iteracion solo documental.

## Comandos ejecutados y resultado esperado
1. Inventario y estado de docs:
```bash
ls -1 docs/tutorials | sort
git status --short
```
Esperado: presencia de `00-07` y `docs/aws/sagemaker-dg.pdf` disponible para versionado.

2. Revision de estructura y fuentes por tutorial:
```bash
for f in docs/tutorials/*.md; do
  echo "===== $f ====="
  rg -n "^#|^##|Fuentes oficiales|APIReference|docs.aws.amazon.com|readthedocs|sagemaker-dg.pdf" "$f"
done
```
Esperado: cada fase con seccion de fuentes oficiales y referencias AWS.

3. Validacion de hardcodes de bucket en fase 02:
```bash
rg -n 'titanic-data-bucket-939122281183-data-science-user' docs/tutorials/02-training-validation.md
```
Esperado: sin hardcodes residuales en pasos operativos.

## Evidencia
1. `docs/tutorials/00-foundations.md` ahora incluye `Resultado minimo esperado` y `Fuentes oficiales (AWS/IaC)`.
2. `docs/tutorials/01-data-ingestion.md` ahora incluye `Resultado minimo esperado` y `Fuentes oficiales (AWS/S3)`.
3. `docs/tutorials/02-training-validation.md` ahora incluye `Fuentes oficiales (SageMaker DG/API)` y elimina hardcodes de bucket en rutas de training/evaluation.
4. `docs/tutorials/03-sagemaker-pipeline.md`, `04-serving-sagemaker.md`, `05-cicd-github-actions.md`, `06-observability-operations.md`, `07-cost-governance.md` incluyen referencia local `docs/aws/sagemaker-dg.pdf` en seccion de fuentes.
5. `docs/tutorials/README.md` incorpora bloque de estado de alineacion AWS por tutorial (one-by-one).

## Riesgos/pendientes
1. La referencia local `docs/aws/sagemaker-dg.pdf` debe mantenerse actualizada cuando AWS publique cambios relevantes.
2. Algunas fases backlog (`05-07`) dependen de implementacion real para validar completamente los enlaces operativos en ejecucion.

## Proximo paso
Versionar `docs/aws/sagemaker-dg.pdf` junto con los cambios documentales y continuar con implementacion operativa de fase 04 (serving) y automatizacion fase 05.
